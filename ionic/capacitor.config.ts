import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'IPKP-IonicV.03',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
